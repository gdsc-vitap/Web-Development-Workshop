var userName

console.log(userName)  // This will return undefined instead of throwing an error
console.log(user_Name) // will throw an error since the variable is not defined yet 
console.log(_userName) // will throw an error since the variable is not defined yet
// used to print on terminal/console

userName = "bleep.bloop"

let user_Name = "bleep.bloop"

const _userName = "bleep.bloop"